This is a Compose Multiplatform demo and tests project.
- To run the demo:
  ```shell
  ./gradlew :composeApp:run
  ```
- To run the tests (the same is used on CI (GitHub Actions))
  ```shell
  ./gradlew :composeApp:jvmTest
  ```
  